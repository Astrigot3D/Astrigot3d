# frozen_string_literal: true

# Specifies the source for downloading gems
source "https://rubygems.org"

# Use the github-pages gem. This automatically includes Jekyll and
# other dependencies that match the GitHub Pages build environment.
# This ensures consistency between your local setup and GitHub.
gem "github-pages", group: :jekyll_plugins

# --- Optional Gem ---
# If you plan to use `bundle exec jekyll serve` locally with Ruby 3.0+
# and it fails complaining about 'webrick', uncomment the following line.
# The github-pages gem might install it automatically, so only add if needed.
# gem "webrick"
